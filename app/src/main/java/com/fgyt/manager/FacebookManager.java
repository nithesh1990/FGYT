package com.fgyt.manager;

import com.facebook.Request;
import com.facebook.Request.Callback;

import android.os.Bundle;

public class FacebookManager {

	
	public static void GetAllPosts(Callback mGetPostsCallback ) {
		Bundle params = new Bundle();
		new Request(session, graphPath, parameters, httpMethod, callback)
	}
}
